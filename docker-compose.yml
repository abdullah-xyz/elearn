version: '3'

services:
  db:
    image: mysql
    restart: always
    ports:
      - '3306:3306'
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_USER=elearn
      - MYSQL_PASSWORD=secret
      - MYSQL_DATABASE=elearn